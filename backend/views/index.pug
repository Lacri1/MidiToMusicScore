extends layout

block content
  h2 MIDI to PDF Converter
  form(action='/convert-midi', method='POST', enctype='multipart/form-data')
    input(type='file', name='midiFile')
    button(type='submit') Convert MIDI to PDF